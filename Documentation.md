1. Tuesday Tasks/Notes (8-25-2026) --> Hours Worked: 3 hrs
    * What I did this today is create a directory in GitHub where I can commit my current code and be able to refer back to it later down the line. I also created a Docker Compose file where I would then be able to create a database using PostgreSQL. Both the Docker Compose and the PostgreSQL are going to be useful because the database will allow the system to track books by the ISBN, title, author, and summary. The summary in this case is going to be optional as the user/reader may not have a summary for the book that they may be entering. This database will also allow the user to track the books that they have read, want to read next, as well as rate the books. The Docker compose file is going to be useful because it will improve workflow and issue resolution and that will lead to increased overall efficiency. The Compose file will also define and manage multi-container apps in one YAML file, this will mean that this file specifies configurations for all our conatiners, dependencies, environment variables, volumes and networks. I was able to commit the docker-compose.yaml file to the repository successfully.

    * After the Docker Compose file and the PostgreSQL were functioning properly, I was able to start creating the potential tables that I would need for my project. I created tables for the author information, book information, reader information, and I created a book tracking table as well. The book tracking information table is going track select information from the first three tables. Once the tables were tested and were able to take example data without any errors, I was able to commit the Postgresql tables to the Github. However, I would needed to perform a cp command to copy the files/directories from it's original location in the DBeaver file to the directory that I want the database commited to.

    * What I learned is that even though I'm attempting to commit everything to my repository I have to keep in mind that not everything is going to be readily accessible. As I learned with attempting to commit my PostgreSQL database that I have to copy the database file to the directory that I want it to be in. What was frustrating was that I couldn't figure out how to copy the database file to the directory in order to commit it to GitHub, however, I was able to do research on how to do it successfully.

1. Wednesday Tasks/Notes (8/26/2026)
    * The first task that I did was to look up a tutorial on how I can connect my PostgreSQL database using Python. I needed to import the load function from the pickle module. This will allow us to implement binary protocols for serializing and de-seralizing a Python Object Structure. I also needed to import psycopg2 and this is the PostgreSQL adapator that is going to allow for the Python to be connected. This will also allow for thread safety (several threads can share the same connection). THe other two imports that were used for the python was the `import os` and the `from dotenv import load_dotenv`
        * The `from dotenv import load_dotenv` is used for environment variables. In order to read the variables from the .env file that we want to use, we have to lead the dotenv. The `import os` will be used as a way to ensure that the environment variables are being properly mapped. Each variable that is in the .env program file is going to be fetched by using the `os.environ.get("variable", "value")`, however, the exception to this rule is the host. DBeaver is going to default to using the localhost as it's going to assume that we're developing on our own device. 
        * I created another file where it was a `.ini` file. This style of file is going to be updated whenever the user or the system makes certain changes to the original or default configurations of the Windows OS. In this case, if a change were to happen to the host where the host is not localhost anymore, then the .ini file will update the file.
    * The second task that I did was a testing phase to ensure that the code that I have written in my python file did work. I was experiencing some errors as it my database was not connected at first, after I performed a `docker compose up` command, it was able to perform with less errors, however, it was not reaching the response I was expecting. However, after some trouble shooting and getting the code to work where it was printing out the printline and the connect method in Ubuntu, I was able to move onto finding a way to not have a password or a user being hard coded in the docker-compose file. The docker-compose.yaml file was the first file that needed to be changed as that was containing hardcoded values. The `${variable}` is going to allow us to substitute the varialbe that is associated with value. For instance, the `POSTGRES_USER: ${DB_USER}` is going have the value of the database user stored into the DB_USER variable.
        * Once the docker-compose file was fully modified, the next file that was modified was the PostgreSQL file. The pieces of code that were modified for the PostgreSQL was just to allow for the author_id to be selected from the author information where the first-name and last_name matched with the corresponding values. The author id and the user_id's were modified to ensure that the id's would autorincrement each time there was a new addition to the database. 
        Another addition was in the book tracking table the user_id, author_id and the book_id's would refer to the parent child and the id's would now be refered to as a foreign key.
    * I learned more about how different file extensions work (such as the .ini file extentsion being used for certain tasks). I also learned about how the different imports in Python can aid in the development process. For instance, importing the psycopg2 into python helps by connecting PostgreSQL to Python. I thought it was interesting how the main features of psycopg2 is the complete implementation of the Python DB API in version 2.0 specifications and there's also thread safety where we can have several threads that can share the same connection. 
    * What was frustrating is that some of the concepts that I learned from my Summer course that covered Docker and Kubernetes was that I had a difficult time fully remebering what the `{DB_USER}` for example was indiicating, but I did some researching on what it meant and after doing some further reading about in on the website, it made a little bit more sense. Another part that was interesting as making the PostgreSQL modifications, it was intriguing that it was possible to have compound `insert into` statements. It wasn't a thought that had occurred to me prior to making the modifications, but it helped cut down on the total amount of lines that were in the database code rather than having one insert into statement that may take up more than one line.
     * It was also interesting that the compound statments that can also have multiple selects as well in the PostegreSQL, this takes some more lines to perform a similar process, but it also helps narrow down which tables to look at and which columns/rows to look at to get the data. For instance, the select statements where I'm looking for the user_id and the book_id where the username and book_isbn are specified as mentioned earlier helped narrow down which book id and which user id for the tracking the book that the user has read. 

1. Friday Tasks (8/28/2026)
    * The first task that I did was vreate a volume for the PostgreSQL database. Another task that I did was start adding code for FastAPI and pytest for my Python code. I also added functions to my main Python file where the FastAPI can print out a list of books, search book details, and a function that will also get the book details. In order to perform some of the commands that I needed, I needed to install pytest in order to import some packages. 
    * What I learned is that if I perfrom the command of `uv run fastapi dev`, this command will launch a FastAPI development server. It will also automatically create or check the virtual environment. Another item that I learned while performing my tasks for today is that if I open a new tab in Ubuntu (the virutual environment that I'm using), and perform a `curl http://google.com` I will get a message providing the information that the contents of that link has been moved to a different location. However, if I perform the same command, with just a `curl http://www.google.com`, it will print the HTML information given the following information. Another portion that I learned is that if I perform  the `code .` and then the `uv run pytest` command, I will be able to test my code. When a test succeeds, a green period mark (.) will appear for each test that was passed. However, in certain circumstances, a red F will appear if a test has failed. However, this will only occur if the test did not meet the expected outcome. If it's just a `F`, it just stands for *Failed (a test assertion or exception raised an error)*. 
    * Something that was frustrating was that I wasn't fully able to understand what errors were occuring throughout the tests, but after troubleshooting and taking breaks when needed, I was able to think through the problems more clearly and I was also able to fully complete my tests. The tests at that point were also able to be passed successfully. 
    * After I made sure the tests were all passing, I was committed what code I had accomplished to my GitHub repository to ensure that it was all as up to date as possible. This is also to ensure that if something were to happen to the code that I currently do have done where it would 'disappear' I would have someplace where the code was stored. This is so then I don't lose time re-doing all of the lines of code that I have done so far. This will also be good for me to refer back to if there was a line of code that was deleted and not replaced right away.
    * The information that I was able to gather was most likely the interesting part about what I have done throughout the day because it was able to help me through some of the tasks that I needed to do. Another part was interesting was how I was able to refresh myself on some Docker information with just being able to look through an old textbook. This was helpful because I was able to understand the concept again after rereading in a few more times to ensure that I was able to fully understand it. 
    * I also had a meeting wht Dr. West to go over some items for the project and he recommended that I can include some verification in my project for the data, playing around with differetn data types (i.e. movies/dvds) in addition to books. We also set up another meeting for September 3rd (10:30 am) to discuss the progress that was made on the project. We also talked about goals and ideas that I had for the project and how those might influence the direction that the project might go further down the line.