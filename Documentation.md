* __Tuesday Tasks/Notes (8-25-2026)__ 
    * What I did this today is create a directory in GitHub where I can commit my current code and be able to refer back to it later down the line. I also created a Docker Compose file where I would then be able to create a database using PostgreSQL. Both the Docker Compose and the PostgreSQL are going to be useful because the database will allow the system to track books by the ISBN, title, author, and summary. The summary in this case is going to be optional as the user/reader may not have a summary for the book that they may be entering. This database will also allow the user to track the books that they have read, want to read next, as well as rate the books. The Docker compose file is going to be useful because it will improve workflow and issue resolution and that will lead to increased overall efficiency. The Compose file will also define and manage multi-container apps in one YAML file, this will mean that this file specifies configurations for all our conatiners, dependencies, environment variables, volumes and networks. I was able to commit the docker-compose.yaml file to the repository successfully.
    * After the Docker Compose file and the PostgreSQL were functioning properly, I was able to start creating the potential tables that I would need for my project. I created tables for the author information, book information, reader information, and I created a book tracking table as well. The book tracking information table is going track select information from the first three tables. Once the tables were tested and were able to take example data without any errors, I was able to commit the Postgresql tables to the Github. However, I would needed to perform a cp command to copy the files/directories from it's original location in the DBeaver file to the directory that I want the database commited to.
        * What I learned is that even though I'm attempting to commit everything to my repository I have to keep in mind that not everything is going to be readily accessible. As I learned with attempting to commit my PostgreSQL database that I have to copy the database file to the directory that I want it to be in. What was frustrating was that I couldn't figure out how to copy the database file to the directory in order to commit it to GitHub, however, I was able to do research on how to do it successfully.

1. __Wednesday Tasks/Notes (8/26/2026)__
* The first task that I did was to look up a tutorial on how I can connect my PostgreSQL database using Python. I needed to import the load function from the pickle module. This will allow us to implement binary protocols for serializing and de-seralizing a Python Object Structure. I also needed to import psycopg2 and this is the PostgreSQL adapator that is going to allow for the Python to be connected. This will also allow for thread safety (several threads can share the same connection). THe other two imports that were used for the python was the `import os` and the `from dotenv import load_dotenv`
    * The `from dotenv import load_dotenv` is used for environment variables. In order to read the variables from the .env file that we want to use, we have to lead the dotenv. The `import os` will be used as a way to ensure that the environment variables are being properly mapped. Each variable that is in the .env program file is going to be fetched by using the `os.environ.get("variable", "value")`, however, the exception to this rule is the host. DBeaver is going to default to using the localhost as it's going to assume that we're developing on our own device. 
    * I created another file where it was a `.ini` file. This style of file is going to be updated whenever the user or the system makes certain changes to the original or default configurations of the Windows OS. In this case, if a change were to happen to the host where the host is not localhost anymore, then the .ini file will update the file.
* The second task that I did was a testing phase to ensure that the code that I have written in my python file did work. I was experiencing some errors as it my database was not connected at first, after I performed a `docker compose up` command, it was able to perform with less errors, however, it was not reaching the response I was expecting. However, after some trouble shooting and getting the code to work where it was printing out the printline and the connect method in Ubuntu, I was able to move onto finding a way to not have a password or a user being hard coded in the docker-compose file. The docker-compose.yaml file was the first file that needed to be changed as that was containing hardcoded values. The `${variable}` is going to allow us to substitute the varialbe that is associated with value. For instance, the `POSTGRES_USER: ${DB_USER}` is going have the value of the database user stored into the DB_USER variable.
    * Once the docker-compose file was fully modified, the next file that was modified was the PostgreSQL file. The pieces of code that were modified for the PostgreSQL was just to allow for the author_id to be selected from the author information where the first-name and last_name matched with the corresponding values. The author id and the user_id's were modified to ensure that the id's would autorincrement each time there was a new addition to the database. 
        Another addition was in the book tracking table the user_id, author_id and the book_id's would refer to the parent child and the id's would now be refered to as a foreign key.
* I learned more about how different file extensions work (such as the .ini file extentsion being used for certain tasks). I also learned about how the different imports in Python can aid in the development process. For instance, importing the psycopg2 into python helps by connecting PostgreSQL to Python. I thought it was interesting how the main features of psycopg2 is the complete implementation of the Python DB API in version 2.0 specifications and there's also thread safety where we can have several threads that can share the same connection. 
* What was frustrating is that some of the concepts that I learned from my Summer course that covered Docker and Kubernetes was that I had a difficult time fully remebering what the `{DB_USER}` for example was indiicating, but I did some researching on what it meant and after doing some further reading about in on the website, it made a little bit more sense. Another part that was interesting as making the PostgreSQL modifications, it was intriguing that it was possible to have compound `insert into` statements. It wasn't a thought that had occurred to me prior to making the modifications, but it helped cut down on the total amount of lines that were in the database code rather than having one insert into statement that may take up more than one line.
* It was also interesting that the compound statments that can also have multiple selects as well in the PostegreSQL, this takes some more lines to perform a similar process, but it also helps narrow down which tables to look at and which columns/rows to look at to get the data. For instance, the select statements where I'm looking for the user_id and the book_id where the username and book_isbn are specified as mentioned earlier helped narrow down which book id and which user id for the tracking the book that the user has read. 

* __Friday Tasks (8/28/2026)__
    * The first task that I did was vreate a volume for the PostgreSQL database. Another task that I did was start adding code for FastAPI and pytest for my Python code. I also added functions to my main Python file where the FastAPI can print out a list of books, search book details, and a function that will also get the book details. In order to perform some of the commands that I needed, I needed to install pytest in order to import some packages. 
    * What I learned is that if I perfrom the command of `uv run fastapi dev`, this command will launch a FastAPI development server. It will also automatically create or check the virtual environment. Another item that I learned while performing my tasks for today is that if I open a new tab in Ubuntu (the virutual environment that I'm using), and perform a `curl http://google.com` I will get a message providing the information that the contents of that link has been moved to a different location. However, if I perform the same command, with just a `curl http://www.google.com`, it will print the HTML information given the following information. Another portion that I learned is that if I perform  the `code .` and then the `uv run pytest` command, I will be able to test my code. When a test succeeds, a green period mark (.) will appear for each test that was passed. However, in certain circumstances, a red F will appear if a test has failed. However, this will only occur if the test did not meet the expected outcome. If it's just a `F`, it just stands for *Failed (a test assertion or exception raised an error)*. 
    * Something that was frustrating was that I wasn't fully able to understand what errors were occuring throughout the tests, but after troubleshooting and taking breaks when needed, I was able to think through the problems more clearly and I was also able to fully complete my tests. The tests at that point were also able to be passed successfully. 
    * After I made sure the tests were all passing, I was committed what code I had accomplished to my GitHub repository to ensure that it was all as up to date as possible. This is also to ensure that if something were to happen to the code that I currently do have done where it would 'disappear' I would have someplace where the code was stored. This is so then I don't lose time re-doing all of the lines of code that I have done so far. This will also be good for me to refer back to if there was a line of code that was deleted and not replaced right away.
    * The information that I was able to gather was most likely the interesting part about what I have done throughout the day because it was able to help me through some of the tasks that I needed to do. Another part was interesting was how I was able to refresh myself on some Docker information with just being able to look through an old textbook. This was helpful because I was able to understand the concept again after rereading in a few more times to ensure that I was able to fully understand it. 
    * I also had a meeting wht Dr. West to go over some items for the project and he recommended that I can include some verification in my project for the data, playing around with differetn data types (i.e. movies/dvds) in addition to books. We also set up another meeting for September 3rd (10:30 am) to discuss the progress that was made on the project. We also talked about goals and ideas that I had for the project and how those might influence the direction that the project might go further down the line.

* __Saturday Tasks (8/29/2026)__
    * What I did today was that I created an API function that would allow users to add books to the database. I also ensure that it would also test properly a test book as well. The test was successful after testing and fixing any errors that arose as well. This function is unique in it has an `except` where if there's an `psycopg2.errors.UniqueViolation`, it will check to see if the connection is not null and it will perform a `connection.rollback()`. The .rollback() method is going to undo any changes during that current transaction. However, if the condition were to fail, it wold raise an exception error and provide a 409 error code (a conflict with the request that we're sending, often cause by improper configuration). It will also return the message of `A book with this ISBN already exists` stating that we already have a book with that exact ISBN in our database.
        * This can help with minimizing the chances of duplicate books in our system with this particular except block of code. 
    * I also created another API function that would allow the users to remove books as well. This process was similar to adding a book, however, the difference between the two functions is that the add function had a `@library_app.post("/books", status_code=status.HTTP_201_CREATED)` rather than having a `@library_app.delete("/books/{book_id})`. Besides the differences with the `.post` and the `.delete`, there was a similar process between the two functions. The function that deletes has a bit more code that is associated with the function as it's going to be removing the data from the table. If we have a book that isn't in the table already, then the function is going to return an error message of `Book not found` with the associated status code of `404`. However, if the book was successful, then the message is going to be different. The message in this case is going to be `Book deleted successfully`. 
        * Doing further research on sending data, specifically for POST, what I learned is that `POST` is the most common way to send data whereas .delete is going to delete the specified data from the database. In the case of the delete function, this is going to delete the book with that specific book_id.
    * What was interesting for this work day is that the rollback method is going to udno any changes that were done during that current transaction. This would make sense in a way to ensure that there would be no drastic changes made to the database without causing significant harm to the actual code. Another part that was interesting was that POST is used to create data and DELETE is used to delete the data in APIs. GET would also be used to read the data, however, we wouldn't want to read the data if we're adding or removing the data from our testing database. A part that was a little bit frustrating is that I wasn't fully able to create some of the functions without the help from outside sources (i.e. the AI chat in VS code or Claude).

* __Sunday Tasks (8/30/2026)__
    * The first task that I did today was to create an API function that would allow users to edit a description of a book. This required a class that was named `Book_Description(BaseModel)`. The BaseModel paramter in the class creation is going to be used as the foundation for defining the data structure, applying validations, and enabling automatic parsing. 
    * This means that when we use BaseModel, the automatic data validation is going to validate the data types automatically when we create an instacne of the model. This can be helpful to catch errors early and ensure the integrity of the data.
    * Another aspect of using BaseModel in the code is that it will also allow for serialization and Parsing. This means that the Pydantic models can easily convert to and from various data formats (such as JSON, dict, etc.), and this makes it good for APIs or data-driven applications.
    * Another positive is custom validations. This is where we can define validation methods (in the case of the description update class, we're validating the book description itself) and this is going help enfore more compelx logic.
        * Finally, once a Pydantic model is instantiated, it is immutable by default, this is to ensure consistency in your data objects.
    * The second task was to create a function in my test file that would ensure that the logic would work properly. There was a function that would check to see if the function in the main code file would work as well and I also created a second function that would test to see if the function would not work. This was to ensure that the code that was added into the `main.py` file would work in both scenarios.
    * Another task that I completed today was to create an Angular project, this was a bit difficult because I either haven't worked with Angular in a while or there were some new ideas that I haven't done before. In order to make sure that I was performing the correct commands, I consulted Google to make sure that the commands and the versions were being installed properly and without many errors. However, another difficult part of this process was trying to find a way to access the files of the Angular project without needing to proceed into a different directory. After testing some different processes of moving the project into a portion of the directory, I returned the project into it's original state.
        * However, after further discussion with a supervisor, the Angular Project is going to be completed in October, as I didn't want a file that was unnecessary until further down the line in the project, I decided to remove the directory and from GitHub. In order to proceed with removing said directory and the files that were apart of that directory, I needed to perform a `rm -r 'directory_name'` command. This was to remove the directory and as I removed it from the project, I also needed to update GitHub to ensure that it wouldn't appear in one place and not the other.
        * The next step in the process was to perfrom a `git add -u` command. The `-u` flag stands for "update" and this will tell Git to automatically stage files that have been modified or deleted locally, een if they aren't there anymore. We would also need to commit the changes with a message as well. Finally, we could push those changes to the main branch (or the branch that we want to push it too).
    * As mentioned before, the most difficult part about my task today was figuring out the Angular project. I would say the interesting part about the task today was learning about how BaseModel works more. Another portion of today's task was being able to use `.put` during the testing process. This was interesting because `.put` creates a new resource or replaces a representation fo the target resouce with the request content.
        * Comparing the `.put` to `.post` is `PUT` is idempotent. This means that calling it once is no different from calling it several times successfully and there are no side effects.

 * __Monday Tasks (8/31/2026)__
    * The first task that I did was creating to create a Dockerfile. This was important because this file is going to provide a blueprint or script of sequential instructions that would automate the creation of a Docker image.
        * This process was interesting because some of the concepts from creating a Docker-Compose.yaml file from when I first started the project. 
        * Another part that was interesing was the `CMD <command>`. This is because this is going to allow us to define the default program that is run once we start the container based on the image that it's creating.
        * It wasn't testing properly, however, I was able to fix the problem as it was needed to be rebuilt with the creation of the `requirements.txt` file
            * I would say attempting to build the Docker file was the frustrating portion about this first task. However, after solving the problem, it was a relief because after testing the link from the Dockerfile, it was a success.
    * Another task that I had today was to also created another API function that would allow the user to search the titles with a single word and it would pull up the titles that have a word in the title.
    * My third task was to create some UI (User Interface) drafts. This was another important task because in a few weeks I need to be creating my User Interface. I was going to attempt to make a few ideas for what I would like to have the UI be, and I was trying to keep in mind accessability and usability for the users. This was because there may be users that may have visual limitations or they may not have consistent connection to the internet.
        * If I had to rank which tasks were harder today, it would probably be the Docker file as I had to perform some research on what was involved in a Docker file even though I was already familiar with Docker as a language. I would say the second hardest task would be a tie between the API function that I also created and trying to find ideas for the User Interface. The second and the third tasks were hard within their own right because the second task required some differnt code than what I was used to, but it was not something that couldn't be looked up or ask about with Claude or the Visual Studio Chat.
        * It was interesting to find which ideas that I liked versus what I didn't like once I started sketching the ideas out on paper. It was difficult at first as I hadn't started thinking about it until today. I've also noticed that there were some similarities between the ideas that I've had. There were some minor differences as I was trying to have a different application/visual effect with the UI.
            * Another part that I found interesting that as I was drafting the User Interface that there were some considerations that I didn't think of at first. For instance, one of my ideas contained a table that had a genre column. This was discussed in the first meeting that I had with my supervisor.
                * This is would be a nice addition as it would allow for even more narrowing in the searching process. Especially if the user is looking for an audio book of the exact same physical copy in the instance that they are going on a roadtrip or if they prefer audiobooks to physical versions.
            * It also didn't occur to me until later in the evening that I could also take into account the variety of media (i.e audiobooks, physical books, games (potentiallly), DVDS for movies).
                * This could also be nice to have in my database as it would further allow for the users to see the different media that they have in the database as well. As this can narrow down the searching parameters, even if there are multiple books that are of a similar genre, the user can then use the genre and a certain word that the title may contain to find the book that they are looking for in a media that they could also be looking for.
            * I thought that these potential new additions were surprising when I was drafting the User Interface. This is because it wasn't something that I thought of when I was initially creating my files and then creating the code for those files. I also thought that this was interesting because as mentioned before, there weren't things that I didn't take into account what could be added to the database that could make the searching the database easier for the users.
            * I also worked on some documentation for my FastAPI file to ensure that I understood the code that I wrote. There's still some documentation that I need to write and read through, but this was another small task that I also tried to complete today as well.

* __Tuesday Task__
    * The first task that I did today was I did some research about what was involved in the Docker file that I created the day before. I did this to try and get a better understanding of the commands were properly doing. What was interesting as I was performing the research for the commands, it was making more sense on why those commands were needed and how they would influence/impact the code that I would be using it for.
    * After finished creating some notes/documentation for the Docker file, my second task that I completed today was to add columns in my book_info table to include 'genre' and 'media'. I did this because it would allow for users to add what kind of media format they have in text and it would also allow them to add what kind of genre(s) the book is.
        * Another intention behind adding these two new columns is that it could also allow for the users to include some board games, video games, movies, or other types of media rather than just having books being included in the database. 
        * This could then allow for the users to see what movies they have to avoid having duplicates of the same items in their library. 
        * This was also another interesting and a bit annoying at the same time as the troubleshooting for the database was making it difficult. This was because I also changed the original name of the table that the two new columns were apart of to be media-based rather than book-based. However, when I went to test the database to ensure that all of the code was working properly, it was providing errors as I hadn't changed the other names from the book-based table to the media based table.
        * However, after changing the original names of the table to the media-based name, it was able to work as it should be. However, upon looking through the code and attempting to continue the association of the code with the rest of the tables, I changed the name of the media-based table back to a book-based name. 
    * After updating my database, I needed to 're-connect' my database as the database was being created in DBeaver. The commands that I needed to use were similar to when I first needed to commit the database. The first command that I need to perform is `cp "/mnt/c/Users/grace/AppData/Roaming/DBeaverData/workspace6/General/Scripts/digital-library-system.sql" ~/Digital-Library-System/`. This command will overwrite the old version in my prject with the latest edit. This command is only going to be performed because I'm editing the file in Ubuntu rather than doing the database in VS code. 
    * The next command that I would need to perform (if I need to change the directory) would be `cd Digital-Library-System` and then I would perform a `git status`. The `git status` would allow us to see our PostgreSQL has been modified. Then we can peform the `git add .` command. We would then need to perform the `git commit -m "message here"`.
        * This was process was also interesting because even though this was completed earlier in the development stages, there were not a lot fo commands that were needed to perform.
    * For my third task, I created more FastAPI functions for the genre and the meida columns from the PostgreSQL database. The first function of searching books by media types is different than some of the other functions. The except block is similar to the other search functions. However, the difference is that this except block is going to be searching by media types. The exception is going to be raised if there is an error with the search by media type.
    * Another task that I did, in the main python file, I added some documentation about what the purpose of the functions are fulfilling for the project. 